import Stack from "@mui/material/Stack";

const AuthElement = () => {
  return (
    <Stack
      direction={{
        sm: "row",
        xs: "column",
      }}
      minHeight="100%"
    ></Stack>
  );
};

export default AuthElement;
